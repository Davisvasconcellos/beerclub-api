# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=beerclub
DB_USER=root
DB_PASSWORD=sua_senha_aqui

# Server Configuration
PORT=4000
NODE_ENV=development

# Public API base URL (used in docs and generated links)
API_PUBLIC_BASE_URL=http://localhost:4000

# JWT Configuration
JWT_SECRET=seu_jwt_secret_super_seguro_aqui
JWT_EXPIRES_IN=24h

# CORS Configuration
# You can set multiple origins separated by comma for production
# Example for prod: https://app.vibesessionsproject.com,https://studio.swagger.io
CORS_ORIGINS=http://localhost:4200

# File Upload
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=5242880

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info 

# Firebase Admin Credentials (Application Default)
# Absolute path to your service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/your/firebase-service-account.json

# Environments
# Create environment-specific files for deployment:
# - .env.development for local development
# - .env.production for production
# The server will load .env.<NODE_ENV> first, then fall back to .env.